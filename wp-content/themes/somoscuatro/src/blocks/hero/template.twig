{% extends "section.twig" %}

{% set background = get_color_name(bg_color) %}
{% set foreground = get_foreground_color_name(background) %}

{% block section_content %}
	<div class="text-{{ foreground }}">
		<div class="container flex flex-col items-center justify-between md:flex-row container-padding">
			<div class="flex flex-col gap-4">
				<h1 class="text-5xl font-medium md:text-6xl font-syne">{{ title }}</h1>
				{% if text %}
					<div class="flex flex-col max-w-lg gap-2 text-xl font-light">{{ text|striptags('<br><p>') }}</div>
				{% endif %}
			</div>
			{% if image %}
				<img src="{{ image.url }}" alt="{{ image.alt }}" width="584" height="640">
			{% endif %}
		</div>
	</div>
{% endblock %}
