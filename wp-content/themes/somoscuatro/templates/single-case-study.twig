{% extends 'base.twig' %}

{% block content %}
	<main id="content">
		<div class="container flex flex-col items-center max-w-6xl gap-4 py-10 mx-auto md:py-20">
			{{ get_breadcrumbs() }}
			<h1 class="text-4xl font-medium md:text-center md:text-6xl font-syne">{{ post.title }}</h1>
		</div>
		{% if post.content is not empty %}
			{{ post.content }}
		{% endif %}
	</main>
{% endblock %}
