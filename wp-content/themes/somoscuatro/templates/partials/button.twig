{% set attrType = {
	hollow: ' border border-black',
	link: '',
} %}

<a href="{{ url }}" target="{{ target }}" {{ target == '_blank' ? 'rel="noopener"' }} class="py-4 w-fit px-7{{ attrType[type] }}">
	{{ title }}
</a>
